e_flag: сократил количество if
key_checker: было дублирование в file_input_format и konsole_input_format, вынес в отдельную функцию
Поменял пару названий переменных
Починил flake8 и mypy, теперь, вроде, проходит все проверки.
Типы переменных есть.
Тип возвращаемого значения тоже есть.
Пока разбираюсь, что такое не интеграционные тесты
Не понял комментарий "а если файл не существует? а если файл не файл?". У меня при вызове функции есть же проверка на это. Если файл есть, то вызывается file_input_format, иначе konsole_input_format.

